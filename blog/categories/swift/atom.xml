<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: swift | NK]]></title>
  <link href="http://nathankot.com/blog/categories/swift/atom.xml" rel="self"/>
  <link href="http://nathankot.com/"/>
  <updated>2015-03-24T17:06:40+09:00</updated>
  <id>http://nathankot.com/</id>
  <author>
    <name><![CDATA[Nathan Kot]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Using monads in Swift]]></title>
    <link href="http://nathankot.com/blog/2015/03/24/using-monads-in-swift/"/>
    <updated>2015-03-24T16:18:52+09:00</updated>
    <id>http://nathankot.com/blog/2015/03/24/using-monads-in-swift</id>
    <content type="html"><![CDATA[<p>One of the coolest features of Swift is that it lets you define your own
operators. This leads to being able to re-implement many operators found in more
functional languages that we know and love.</p>

<p><a href="https://github.com/typelift/Swiftz">Swiftz</a> is a new<sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup> library that implements a handful of essential and obscure
operators, as well as many structs that we take for granted in functional
environments. Today I&rsquo;m going to take a brief look at the <code>Maybe</code> monad provided
by Swiftz.</p>

<p>Let&rsquo;s implement a game of chinese whispers. But in this version, any participant
can give up on the game. We have the participants, each a function that <em>might</em>
return a phrase that they think is correct, or nothing at all:</p>

<p>```swift
func participant1(phrase: String) &ndash;> Maybe<String> {
  return Maybe.just(phrase)
}</p>

<p>func participant2(phrase: String) &ndash;> Maybe<String> {
  return Maybe.just(&ldquo;aloha&rdquo;)
}</p>

<p>func participant3(phrase: String) &ndash;> Maybe<String> {
  return Maybe.just(&ldquo;(phrase) i think&rdquo;)
}</p>

<p>func participant4(phrase: String) &ndash;> Maybe<String> {
  return Maybe.none()
}
```</p>

<p>Now how do we write a function that takes a phrase and an ordered list of
participants, and then returns the result? We could do this:</p>

<p>```swift
func play(initialPhrase: String, participants: [String &ndash;> Maybe<String>]) {
  var result: Maybe<String> = Maybe.just(initialPhrase)
  for participant in participants {</p>

<pre><code>if result.isNone() { break }
result = participant(result.fromJust())
</code></pre>

<p>  }
  return result
}
```</p>

<p>You&rsquo;ll notice that we are checking for <code>isNone()</code> on each iteration because we
don&rsquo;t want to pass the next participant anything if the game has already
stopped. This is the problem that Monad&rsquo;s solve, given the context of <code>Maybe</code> it
<em>knows</em> how to handle the case of nothing or something:</p>

<p>```swift
func play(initialPhrase: String, participants: [String &ndash;> Maybe<String>]) {
  var result: Maybe<String> = Maybe.just(initialPhrase)
  for participant in participants {</p>

<pre><code>// &gt;&gt;- is the equivalent of &gt;&gt;= from haskell, it's
// called the `bind` operator
result = result &gt;&gt;- participant
</code></pre>

<p>  }
  return result
}
```</p>

<p>Okay. That saved about one line, but we can make the entire thing more
functional:</p>

<p>```swift
func play(initialPhrase: String, _ transform: String &ndash;> Maybe<String>) {
  return transform(initialPhrase)
}</p>

<p>// And usage looks like:
play(&ldquo;hello there&rdquo;, { phrase: String &ndash;> Maybe<String> in
  return Maybe.just(phrase) >>&ndash; participant1 >>&ndash; participant2 >>&ndash; participant3
})</p>

<p>// Or, IF Swift supported partial operator application:
play(&ldquo;hello there, (>>&ndash; participant1 >>&ndash; participant2 >>&ndash; participant3) • Maybe.just)
// • is an operator defined by Swiftz (compose)
```</p>
<div class="footnotes">
<hr/>
<ol>
<li id="fn:1">
<p>Swiftz is not well documented yet, but the source code is clean enough to understand what&rsquo;s going on.<a href="#fnref:1" rev="footnote">&#8617;</a></p></li>
</ol>
</div>

]]></content>
  </entry>
  
</feed>
